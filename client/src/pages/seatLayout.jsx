import React from 'react'
import { useParams } from 'react-router-dom'

const SeatLayout = () => {
  const{id ,date} = useParams()
  const [selectedSeats , setSelectedSeats] =use

  return (
    <div>

    </div>
  )
}

export default SeatLayout